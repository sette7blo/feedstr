---
name: Agent task
about: One unit of work, spec'd so a coding agent can start with no other context
title: ''
labels: needs-spec
assignees: ''
---

## Goal

<!-- One or two sentences. What should be true when this is done? Not how. -->

## Context

<!-- Why this exists. Link the roadmap pass, prior issue, or the user-visible symptom.
     Assume the reader has never seen this repo before. -->

## Files likely involved

<!-- Best-guess paths, e.g. public/app/columns.js, public/styles.css.
     A guide, not a constraint — say so if it is a guess. -->

## Acceptance criteria

<!-- Checkable statements. The coding agent closes the issue only when all are true. -->

- [ ]
- [ ]

## Out of scope

<!-- Explicitly list adjacent work this task must NOT touch. This is the most
     important section: it is what stops an agent from widening the change. -->

## Verification

<!-- How to prove it works. Name the command, the URL, and the device if the
     check is visual (e.g. "iPhone portrait, home-screen PWA"). -->

## Notes for the implementer

<!-- Repo conventions that apply here. Common ones:
     - Bump the ?v= cache-buster in public/index.html for any edited JS/CSS
     - Rebuild with `docker compose up -d --build` and confirm the container is healthy
     - Add a CHANGELOG.md entry under [Unreleased] if a self-hoster would care
     - No emojis anywhere; no AI attribution in commits or code comments -->
